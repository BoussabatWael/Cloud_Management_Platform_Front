import { core_users } from './core_users';

export class core_users_instances {
  id!: number;
  user_id!: core_users;
  element!: number;
  element_id!: number;
  status!: number;
  start_date!: Date;
  end_date!: Date;
}
